# Copiar a `.env` y ajustar valores. No commitear `.env` (AGENT.md §2.7).
POSTGRES_DB=tickets
POSTGRES_USER=tickets
POSTGRES_PASSWORD=change_me

# JDBC (Spring Boot — application.yml)
DB_HOST=localhost
DB_PORT=5432
